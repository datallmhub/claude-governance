# Personal Overrides — do not commit (added to .gitignore)

## Communication style
- Short, direct responses. No end-of-response summary.
- Do not rephrase my question before answering.
- Propose one solution, not a list of options unless explicitly asked.

## Personal workflow
- Always read the full file before modifying it.
- Always verify compilation (`npm run build` in backend and `npm run typecheck` in frontend) before declaring a task done.
- When intent is unclear, ask ONE targeted question.
- If blocked after 2 failed attempts on the same problem, stop and tell the user to run `/model claude-opus-4-8`. Do not attempt a third fix.

## Claude Code session best practices

Context management:
- `/compact` — compress conversation when context grows long (long sessions, big diffs)
- `/clear` — reset session entirely when switching to an unrelated task

Model cost optimization (manual — no automatic routing):
- `/model claude-haiku-4-5` — documentation, renaming, simple syntax fixes
- `/model claude-sonnet-4-6` — feature development, bug fixes, architecture (default)
- `/model claude-opus-4-8` — blocked problems, complex refactoring, security review

## Local environment
- Backend on port 3000, frontend on port 5173.
- Local config: `backend/.env.local` (ignored by Git).
- Local database: `taskflow_dev` on PostgreSQL localhost:5432.
- Redis on localhost:6379 for Bull queue (optional in dev).

## Code preferences
- Prefer NestJS `ConfigModule` over inline env reads.
- Use `nestjs-paginate` or `nestjs-typeorm-paginate` for list endpoints.
- On the frontend, prefer arrow functions over function declarations.

## Work in progress
- Evaluating Prisma as alternative to TypeORM for new modules.
- Adding WebSocket gateway for real-time task notifications.
